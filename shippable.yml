language: java

jdk:
  - oraclejdk8

build:
  ci:
    - MAVEN_HOME=$(mvn --version | grep -h 'Maven home:' | awk '{print $3}')
    - echo $MAVEN_HOME
    - ls ~/.m2
    - mvn -B -f $SHIPPABLE_BUILD_DIR/pom.xml -U clean deploy
